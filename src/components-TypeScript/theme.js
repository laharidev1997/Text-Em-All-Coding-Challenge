
import { createMuiTheme } from "@material-ui/core/styles";
const theme = createMuiTheme({
    palette: {
      primary: {
        main: "#37474f",
        contrastText: "#fff"
      }
    },
    typography: {
      fontSize: 16
    },
    spacing: 2
  });
  export default theme